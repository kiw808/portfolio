{% import '_macros/admin-dashboard.twig' as dash %}

{% set currentRoute = app.request.get('_route') %}

<aside class="min-h-screen fixed px-4 w-64 bg-indigo-600">
    <div class="flex items-center justify-center mb-6 mt-2">
        <svg class="h-6 w-6 fill-current text-gray-100 mr-4">
            <use xlink:href="{{ asset('build/icons/zondicons-sprite.svg#coffee') }}"></use>
        </svg>
        <h3 class="text-2xl py-4 font-bold text-gray-100">Admin</h3>
    </div>
    <nav class="w-full text-gray-100">
        <ul>
            {{ dash.item('Dashboard', 'admin_index', 'dashboard', currentRoute) }}
            {{ dash.item('Blog', 'admin_article_index', 'news-paper', currentRoute) }}
            {{ dash.item('Projects', 'admin_project_index', 'portfolio', currentRoute) }}
            {{ dash.item('Clients', 'admin_client_index', 'currency-dollar', currentRoute) }}
            {{ dash.item('Technos', 'admin_technology_index', 'code', currentRoute) }}
            {{ dash.item('Users', 'admin_user_index', 'user-solid-circle', currentRoute) }}
            {{ dash.item('API', 'admin_api_sandbox_index', 'pylon', currentRoute) }}
        </ul>
    </nav>
</aside>