{% extends 'front/layout/base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
<div class="container mx-auto p-4">
    <div class="">
        <h1 class="text-6xl">PIERRE MULLER</h1>
        <p class="text-4xl">web developper</p>
        <div>
            {% for techno in technos %}
                <span class="badge badge-indigo hover:bg-indigo-500 hover:text-white">{{ techno.name }}</span>
            {% endfor %}
        </div>
    </div>
</div>
{% endblock %}
